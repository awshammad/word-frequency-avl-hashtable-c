# word-frequency-avl-hashtable-c
Menu-driven C program that reads words from input.txt, counts frequencies using an AVL tree, builds a hash table (chaining) for fast lookup, and provides insert/delete/search + frequency statistics.
